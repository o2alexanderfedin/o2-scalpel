# P3 - Rope vs PEP 695 / 701 / 654 syntax

**Per-PEP outcome:**

- PEP 695 (type aliases, line 10): ok
- PEP 701 (nested f-strings, line 14): ok
- PEP 654 (except groups, line 20): ok

**Runtime:** Python 3.13.3 (>=3.12: True)
**pylsp-rope plugin loaded:** True

**Evidence:**

- diagnostics published: 0
- extract_method errors: PEP695=None; PEP701=None; PEP654=None
- raw diagnostics: []

**Decision:** All-pass -> declare Python 3.10-3.13+ supported.

**Signal vs noise:**

- `source: 'pylsp'` + 'syntax' on a PEP line = parse stack rejected.
- extract errors w/ 'syntax'/'parse'/'rope' = Rope fault; generic semantic refusals do not count.
- mypy `return in except*` = SEMANTIC (mypy parsed it), not parse failure.
