# P1 - pylsp-rope unsaved-buffer behavior

**Outcome:** A - pylsp-rope honors didChange in-memory; no extra didSave needed

**Evidence:**

- pylsp-rope plugin loaded (executeCommandProvider has `pylsp_rope.*`): True
- Code actions surfaced at `def plus(` position: 20 (pylsp-rope-typed: 20)
- Inline executeCommand error: None
- workspace/applyEdit reverse-requests captured: 1
- Inline WorkspaceEdit newText (concatenated): '_TEST_CALL = 1 + 2\n'
- Inlined call site `_TEST_CALL = 1 + 2` present (in-memory-only signal): True; 'add' (no 'plus') present: False

**API audit (2026-04-24):**

- Wrapper gap: `SolidLanguageServer` PYTHON adapter is Pyright (ls_config.py:346), NOT pylsp. No pylsp adapter exists in `src/solidlsp/language_servers/`. Test bypasses wrapper via raw stdio JSON-RPC.
- pylsp-rope code actions are command-typed (refactoring.py:72-81) with generic titles (`Extract method`, `Inline method/variable/parameter`); identifier-classification requires inspecting executeCommand WorkspaceEdit text, not action titles (plan §4 illustrative classifier won't work as written).
- pylsp-rope reads via `workspace.get_maybe_document(uri).source` (project.py:181-191), i.e., the in-memory pylsp document buffer; transitively, didChange honoring depends on pylsp updating that buffer.

**Decision:**

- A -> Stage 1E pythonStrategy passes the buffer via `didChange` only.
- B -> Stage 1E pythonStrategy injects `didSave({includeText: true})` before every code-action call (+~40 LoC, +1 RTT per call).
- INDETERMINATE -> file pylsp-rope plugin-loading or inline-handler issue as Phase 0 finding; re-run with broader inline target or different command.
