# P6 - pylsp vs basedpyright textDocument/rename convergence

**Outcome:** DIVERGENT - pylsp=2 bp=2 only_in_pylsp=2 only_in_basedpyright=2; merger picks pylsp per scope-report §11.1 and logs provenance.disagreement

**Symbol:**

- File: `vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py`
- Token: `add` (function name on `def add(a: int, b: int) -> int:`)
- Position: line=6 (0-indexed), character=4
- New name: `plus`

**Per-LSP edit set sizes:**

- pylsp: 2 edit tuple(s); request-error: ''
- basedpyright: 2 edit tuple(s); request-error: ''

**Symmetric difference:**

- only_in_pylsp: 2
- only_in_basedpyright: 2
- total symmetric difference: 4

**Sample edits (first 5, sorted; tuple = uri, sl, sc, el, ec, newText):**

- pylsp:
```python
[('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 0, 0, 19, 0, '"""Minimal seed package used by Phase 0 spikes."""\nfrom typing import Final\n\nVERSION: Final = "0.0.0"\n\n\ndef plus(a: int, b: int) -> int:\n    return a + b\n\n\ndef mul(a: int, b: int) -> int:\n    return a * b\n\n\ndef _private_helper(x: int) -> int:\n    return -x\n\n\n__all__ = ["VERSION", "add", "mul"]\n'), ('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__main__.py', 0, 0, 4, 0, 'from . import plus\n\nif __name__ == "__main__":\n    print(plus(2, 3))\n')]
```
- basedpyright:
```python
[('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 6, 4, 6, 7, 'plus'), ('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 18, 23, 18, 26, 'plus')]
```
- only_in_pylsp:
```python
[('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 0, 0, 19, 0, '"""Minimal seed package used by Phase 0 spikes."""\nfrom typing import Final\n\nVERSION: Final = "0.0.0"\n\n\ndef plus(a: int, b: int) -> int:\n    return a + b\n\n\ndef mul(a: int, b: int) -> int:\n    return a * b\n\n\ndef _private_helper(x: int) -> int:\n    return -x\n\n\n__all__ = ["VERSION", "add", "mul"]\n'), ('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__main__.py', 0, 0, 4, 0, 'from . import plus\n\nif __name__ == "__main__":\n    print(plus(2, 3))\n')]
```
- only_in_basedpyright:
```python
[('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 6, 4, 6, 7, 'plus'), ('file:///Volumes/Unitek-B/Projects/o2-scalpel/vendor/serena/test/spikes/seed_fixtures/calcpy_seed/calcpy_seed/__init__.py', 18, 23, 18, 26, 'plus')]
```

**Wrapper-gap findings (re-confirmed from P1/P4/P5a):**

- `Language.PYTHON` resolves to PyrightServer (`src/solidlsp/ls_config.py:346`); neither pylsp nor basedpyright has a `SolidLanguageServer` adapter. Test bypasses the wrapper via `_pylsp_client.PylspClient` + `_basedpyright_client.BasedpyrightClient`. Stage 1E `PythonStrategy` must add real adapters (~50 LoC each, template `jedi_server.py`).
- basedpyright requires the server->client request auto-responder for `workspace/configuration` / `client/registerCapability` / `window/workDoneProgress/create`; `BasedpyrightClient` already handles all three so `textDocument/rename` returns normally.

**Decision (per scope-report §11.1, fixed):**

- CONVERGENT -> multi-server merger sees 0 disagreement on this symbol; no `provenance.disagreement` warning fires; either LSP's edit set is canonical.
- DIVERGENT -> merger picks pylsp for `textDocument/rename` and logs a `provenance.disagreement` warning carrying the symmetric-difference summary (only_in_pylsp / only_in_basedpyright counts + first-N samples). The MVP `RefactorResult` surface should expose this delta so callers can audit the merge.
- INDETERMINATE -> file as a Phase 0 finding; Stage 1D merger must tolerate one or both LSPs returning empty/null rename results (e.g. unsupported file types, server boot failures).
