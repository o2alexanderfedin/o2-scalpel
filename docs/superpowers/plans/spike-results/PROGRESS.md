# Phase 0 — Execution Progress Ledger

> **Purpose:** Durable across context resets. Each task entry records what was done, problems hit, and decisions made. Future sessions check this file FIRST before re-running anything. The TaskList tool tracks per-conversation status; this file tracks cross-conversation history.

**Plan:** [`docs/superpowers/plans/2026-04-24-phase-0-pre-mvp-spikes.md`](../2026-04-24-phase-0-pre-mvp-spikes.md)
**Branch:** `feature/phase-0-pre-mvp-spikes` (git-flow)
**Started:** 2026-04-24
**Author:** AI Hive(R)

---

## Status legend

- ⏳ pending — not started
- 🚧 in-progress — implementer dispatched
- 🔍 spec-review — implementer DONE, spec reviewer dispatched
- 🔬 quality-review — spec reviewer ✅, quality reviewer dispatched
- ✅ done — both reviews ✅, committed
- ⚠️ blocked — implementer escalated; needs decision
- 🔁 fix-loop — reviewer found issues; implementer fixing

## Per-task ledger

| # | Task | Status | Commit | Outcome / Notes |
|---|---|---|---|---|
| 1 | Bootstrap scaffolding + seed fixtures | ✅ | parent 4936ef8 / submod 32e7afdb | scaffolding + seed fixtures + sync conftest fix; both reviews ✅ |
| 2 | Spike S1 — `$/progress` forwarding (BLOCKING) | ✅ | parent 1c54f367 / submod 3e24e449 | Outcome **A**: 140-180 `$/progress` events with 7 rich token classes (rustAnalyzer/Fetching, Building CrateGraph, Loading proc-macros, cachePriming, Roots Scanned, Building compile-time-deps, rust-analyzer/flycheck/N) reach the wrapper dispatcher. Public-API tap is clobbered by `rust_analyzer.py:720` `do_nothing` + single-callback dispatcher → +30 LoC shim required (plan §13 fallback). |
| 3 | Spike S3 — `applyEdit` reverse-request (BLOCKING) | ✅ | parent d4ad50a / submod 3b90edd3 | Outcome **B** (on calcrs_seed/lib.rs): rust-analyzer returns deferred-resolution actions; after `codeAction/resolve`, all 9 surfaced actions are `edit:`-typed (no `command:`-typed). 0 `workspace/applyEdit` reverse-requests fire. Minimal `{applied: true, failureReason: null}` stub sufficient on this fixture; re-verify in S4/S5/S6 with richer assist surfaces. Wrapper-gap finding: no `request_code_actions` / `resolve_code_action` / `execute_command` facade on `SolidLanguageServer`. |
| 4 | Spike P1 — pylsp-rope unsaved buffer (BLOCKING) | ✅ | parent 7277f2aa / submod 3d971fb1 | Outcome **A**: pylsp-rope reads from the in-memory pylsp document buffer. After `didChange` (no `didSave`) renaming `add`→`plus` plus an injected `_TEST_CALL = plus(1, 2)` call site, `pylsp_rope.refactor.inline` triggers a `workspace/applyEdit` reverse-request whose `WorkspaceEdit` contains `_TEST_CALL = 1 + 2` — a string that exists ONLY in the in-memory buffer. 20 pylsp-rope-typed code actions surfaced; rope plugin loads via `pylsp` entry-point group. No extra `didSave` round-trip needed in Stage 1E pythonStrategy. |
| 5 | Spike P2 — organize-imports merge winner (BLOCKING) | ✅ | parent 712cf54a / submod e44cd52e | Outcome **DIVERGENT** (both LSPs return `source.organizeImports`, but with materially different behaviors): pylsp-rope is `command:`-typed (`pylsp_rope.source.organize_import`), ships `WorkspaceEdit` via `workspace/applyEdit` reverse-request, and **removes** unused imports (296 byte output, no `os`/`sys`/`List`); ruff is `edit:`-typed under hierarchical sub-kind `source.organizeImports.ruff`, ships edit inline, and **sorts only** (340 byte output, retains `os`/`sys`/`List` — removal lives under `source.fixAll.ruff`). Decision per §11.1 fixed: ruff wins at merge time; pylsp-rope's organize-imports dropped. Wrapper-gap finding: the remove-vs-no-remove behavioral split means a v1.1 `engine: {ruff, rope}` config knob is desirable for users who want stronger organize behavior. |
| 6 | Spike P5a — pylsp-mypy stale-rate (BLOCKING) | ✅ | parent 916297f5 / submod 334bb3a8 | Outcome **C**: stale_rate 8.33% (1/12; only step 1 stale, oracle=1 pylsp=0) AND p95 latency 8.031s. Cold-daemon hit on first `didSave` exceeded the 8s wait window — pylsp-mypy didn't publish a `mypy`-sourced diagnostic before the loop oracle ran. Steps 2-12 converge perfectly once warm. Q1 falsifier criteria fail (≥5% stale rate AND ≥3s p95 latency). Decision per Q1 §6.3 fallback: drop pylsp-mypy from MVP active server set; basedpyright remains sole type-error source per §11.1. Wrapper-gap: SolidLanguageServer has no `notify_did_change_configuration` facade; Stage 1E `PylspServer` adapter must add one. |
| 7 | Spike S2 — snippetTextEdit:false honored | ✅ | parent 7b588f9b / submod 0623e2d8 | Outcome **A**: 0 of 43 resolved code actions emit `$N`/`${N` snippet markers across a 10-range sweep over `lib.rs` (43 raw == 43 resolved; functions, struct, impl, mod, use, tests covered). Defensive `$N` strip in applier (~10 LoC) is sufficient; no mandatory regex strip path needed. Wrapper-gap finding: the fork hard-codes `experimental.snippetTextEdit: True` at `rust_analyzer.py:458` — there is no public capability-override hook on `SolidLanguageServer`/`RustAnalyzer`, so the test monkey-patches `_get_initialize_params` on the instance pre-`start_server()`. Stage 1A must expose a per-language capability override. |
| 8 | Spike S4 — `experimental/ssr` upper bound | ✅ | parent 5e95a83 / submod fce6f193 | Outcome **feature-unavailable (LSP -32601)** on rust-analyzer 1.95.0 stock build: `workspace/executeCommand` with `command: "experimental/ssr"` returns LSP error `-32601` ("unknown request"). edit_count=0, rss_delta=448kB (post-fix re-run), json_bytes=0. Spike passes (NON-BLOCKING; documents bounds). **Fix loop applied: classifier + dynamic version** — narrowed `except Exception` to `except SolidLSPException`, added `_classify_failure(exc) -> (failure_class, lsp_code)` parsing JSON-RPC -326XX codes (32601→feature-unavailable, 32602→test-bug-invalid-params, 32603→server-crash-internal-error), and replaced hard-coded `1.95.0 (59807616 2026-04-14)` literal with runtime capture via `subprocess.run(["rust-analyzer", "--version"], timeout=5)` (fallback "unknown"). Wrapper-gap (S3 confirmed): no `execute_command` facade on `SolidLanguageServer`; test drops to `srv.server.send_request(...)`. Decision per plan §13 fixed: ship `max_edits: int = 500` on `scalpel_rust_ssr`; gate behind runtime capability probe so it errors gracefully on builds without SSR enabled. Seed crate (2 `+` sites) is a LOWER BOUND regardless. Initial commits before fix loop: parent 8b74c0d / submod 0817c873. |
| 9 | Spike S5 — `expandMacro` on proc macros | ✅ | parent 820fc20 / submod faea1220 | Outcome **feature-unavailable (LSP -32601)** on rust-analyzer 1.95.0 stock build: both `decl_macro!(zero)` (line 47) and `#[derive(Debug, Clone)]` (line 49) positions return `-32601` "unknown request" for `workspace/executeCommand` with `command: "rust-analyzer/expandMacro"`. Mirrors S4's `experimental/ssr` finding — same build is missing both LSP-extensions commands. Reused S4's `_classify_failure` + `_capture_rust_analyzer_version` helpers; appended `#[cfg(feature = "_spike_proc_macro")]`-gated proc-macro target to `seed_fixtures/calcrs_seed/src/lib.rs`. Decision: facade unavailable until rust-analyzer is built with `expandMacro` support. Wrapper-gap (S3/S4 confirmed): no `execute_command` facade on `SolidLanguageServer`; test drops to `srv.server.send_request(...)`. |
| 10 | Spike S6 — auto_import resolve shape | ⏳ | — | — |
| 11 | Spike P3 — Rope vs PEP 695/701/654 | ⏳ | — | — |
| 12 | Spike P4 — basedpyright relatedInformation | ⏳ | — | — |
| 13 | Spike P6 — three-server rename convergence | ⏳ | — | — |
| 14 | Spike P3a — basedpyright==1.39.3 baseline | ⏳ | — | — |
| 15 | Spike P-WB — workspace-boundary rule | ⏳ | — | — |
| 16 | Synthesis — SUMMARY.md | ⏳ | — | — |
| 17 | Phase 0 exit gate + tag | ⏳ | — | — |

## Decisions log (chronological)

| Date | Decision | Rationale | Affects |
|---|---|---|---|
| 2026-04-24 | Phase 0 executed via subagent-driven-development skill, serial dispatch (no parallel implementers). | Per skill rule: "Never dispatch multiple implementation subagents in parallel — conflicts." | All Phase 0 tasks |
| 2026-04-24 | Feature branch `phase-0-pre-mvp-spikes` (git-flow), not worktree. | Project CLAUDE.md mandates git-flow; worktree is the superpowers default but project convention takes precedence. | All Phase 0 commits |
| 2026-04-24 | Each spike test asserts `outcome` is truthy (always passes), not the optimistic outcome. | Spikes record evidence, not features. Failing the test would mean we couldn't continue; classifying A/B/C lets us proceed and feed Stage 1 design. | All spike tests |
| 2026-04-24 | Task 1 spike scaffolding committed inside `vendor/serena` submodule (on `main` branch, writable, not detached); parent commit records the bumped submodule SHA `6c704f2b`. | Submodule was on writable `main` branch at task start, so files were committed there per plan rather than escalating. Parent commit only carries the submodule pointer + `.gitkeep` + this ledger. | Task 1 + future submodule-touching tasks |
| 2026-04-24 | **Pre-commit hook installed in BOTH parent (`.git/hooks/pre-commit`) AND submodule (`.git/modules/vendor/serena/hooks/pre-commit`)** to enforce git-flow on both repos. Per `ensure-git-repo-setup` skill. | User directive "ensure init and enforce git flow" applies to both repos. Direct commits to `main`/`develop` blocked; feature/release/hotfix/bugfix branches required. | All future commits on both parent and submodule |
| 2026-04-24 | **Canonical submodule-commit pattern:** create `feature/<name>` in submodule, commit there, fast-forward-merge into `main`, then bump parent submodule pointer. | P5a implementer discovered + demonstrated this is the only way to get spike work into submodule `main` after the pre-commit hook landed mid-Phase-0. T1–T5 used direct-to-main commits before the hook existed; T6+ use this feature-branch pattern. | Submodule-touching tasks T7–T15+ |
| 2026-04-24 | Submodule local git config pinned to `AI Hive(R) <af@o2.services>` after Task 1 (`git -C vendor/serena config user.{name,email}`). | Task 1 implementer found the submodule's local config defaulted to `o2alexanderfedin`, requiring `--amend --author` per commit. One-time fix prevents recurrence. | All future submodule-touching tasks |
| 2026-04-24 | **PROGRESS.md updates ship as separate commits, NOT amends.** Convention change after Task 1 surfaced an unavoidable SHA-drift cycle when amending the same commit to embed its own SHA. | Implementer correctly identified that every amend rewrites the SHA, so the embedded SHA goes stale immediately. Solution: implementer commits the work, then commits the PROGRESS.md update separately referencing the work commit. Two commits per task is acceptable. | All future tasks |
| 2026-04-24 | Spike test code blocks in plan Tasks 2-15 are **illustrative**, not authoritative. Each spike implementer must verify wrapper methods (`request_code_actions`, `notify_did_change`, `execute_command`, `server.on_notification`, etc.) against the actual `SolidLanguageServer` API before invoking them. | Quality reviewer caught that `start_session()` doesn't exist on the wrapper; broader audit suggests other plan-doc method names are speculative. Missing wrapper methods are themselves Phase 0 findings (they imply Stage 1A must add them). | Tasks 2–15 spike implementers |
| 2026-04-24 | Plan doc Task 1 Step 7 + spike API NOTE updated to reflect canonical sync `with srv.start_server(): yield srv` pattern (verified at `vendor/serena/src/solidlsp/ls.py:717` and existing `test/solidlsp/scala/test_scala_language_server.py:24`). | Same defect was baked into the plan; future re-reads would reproduce the bug. Fixed at source. | All future spike work |
| 2026-04-24 | `LanguageServerConfig` field is `code_language`, NOT `language`. Conftest fix folded into Task 2 submodule commit. | Task 1 conftest had `LanguageServerConfig(language=Language.RUST)` which raised `TypeError`. Verified canonical name at `src/solidlsp/ls_config.py:596` and confirmed in `test/solidlsp/scala/test_scala_language_server.py`. | Conftest + all future spike fixtures |
| 2026-04-24 | **Wrapper `$/progress` plumbing requires a +30 LoC notification-tap shim** (plan §13 fallback). The dispatcher at `solidlsp/ls_process.py:507` is single-callback-per-method (last-write-wins); `rust_analyzer.py:720` pre-registers `do_nothing` for `$/progress` during `_start_server()`, swallowing 140-180 rich progress events per init. Public-API client taps registered after `start_server()` yields see 0 events. The JSON-RPC layer DOES forward all packets — only the dispatch surface is the bottleneck. | Discovered during S1 by wrapping `server._notification_handler` BEFORE `start()` on a second instance (probe 2). Outcome A confirmed with rich token set: rustAnalyzer/{Fetching, Building CrateGraph, Loading proc-macros, cachePriming, Roots Scanned, Building compile-time-deps}, rust-analyzer/flycheck/N. | Stage 1A `wait_for_indexing()` design + S2/S3/S4/S5/S6 spike implementers (same dispatcher pattern affects every notification-listening spike) |
| 2026-04-24 | Spike tests on this host must `os.environ.setdefault("CARGO_BUILD_RUSTC", "rustc")` BEFORE booting rust-analyzer. | Host `~/.cargo/config.toml` has `[build] rustc = "rust-fv-driver"` which crashes on missing dylib (`librustc_driver-f9453740c55d2f61.dylib`), aborting `cargo metadata` during rust-analyzer's project-model load. Without the env override, `_start_server()` partially succeeds but emits 0 progress events. | All Rust spikes (S1-S6) |
| 2026-04-24 | **rust-analyzer returns deferred-resolution code actions**: top-level `textDocument/codeAction` response carries only `{title, kind, data, group?}`; `command`/`edit` populate ONLY after `codeAction/resolve`. Stage 1A code-action flow MUST resolve before classifying `command:` vs `edit:`. | Discovered during S3: initial probe found 4 actions with neither `command` nor `edit` set; resolve step materialized `WorkspaceEdit` directly into the `edit` field for all 9 surfaced actions on the seed crate. Without resolve, every action would be misclassified. | Stage 1A code-action facade design + S2/S4/S5/S6 spike implementers |
| 2026-04-24 | **Wrapper gap**: `SolidLanguageServer` exposes no facade for `textDocument/codeAction`, `codeAction/resolve`, or `workspace/executeCommand`. S3 test drops to `srv.server.send_request(...)` directly. | Verified by grep over `src/solidlsp/ls.py` — only `request_definition`/`request_references`/`request_document_symbols`/`request_rename_symbol_edit` etc. are exposed. The lower-level `LanguageServerRequest` (in `ls_request.py`) DOES have `code_action`/`resolve_code_action`/`execute_command`, so Stage 1A facade work is a thin wrapper-method addition, not a deeper plumbing change. | Stage 1A scope (facade methods) + S2/S4/S5/S6 spike implementers (must drop to `srv.server.send_request` or `srv.server.send.code_action(...)` for now) |
| 2026-04-24 | **Wrapper gap**: `Language.PYTHON` resolves to **PyrightServer** (`ls_config.py:346`); pylsp + pylsp-rope have NO adapter in `src/solidlsp/language_servers/`. The conftest `python_lsp_pylsp` fixture would actually launch Pyright (which doesn't load pylsp-rope). P1 test bypasses the wrapper entirely with a 60-line raw stdio JSON-RPC client. Stage 1E pythonStrategy MUST add a `PylspServer(SolidLanguageServer)` adapter (template: `jedi_server.py`, ~50 LoC). | Confirmed during P1: tried instantiating via `SolidLanguageServer.create(LanguageServerConfig(code_language=Language.PYTHON), ...)` — booted pyright-langserver, not pylsp. The `pylsp_rope` plugin loads correctly via the `pylsp` entry-point group when pylsp itself runs (verified `executeCommandProvider.commands` lists `pylsp_rope.refactor.*` after `initialize`). | Stage 1E scope (add `PylspServer` adapter; ~50 LoC, mirrors `JediServer`) + P2/P3/P4/P5a/P6 spike implementers (must reuse the P1 raw-stdio pattern OR add the adapter first) |
| 2026-04-24 | **pylsp-rope code-action titles are static, identifier-agnostic**: `Extract method`, `Extract method including similar statements`, `Inline method/variable/parameter`, `Use function`, `To method object`, `Convert local variable to field`, `Organize import`, `Introduce parameter`, ... (`pylsp_rope/plugin.py:pylsp_code_actions` + `refactoring.py:CommandRefactor*.get_code_actions`). Plan §13 P1's title-based A/B classifier (`"plus" in title` vs `"add" in title`) **does not work** — titles never contain the target identifier. P1 test classifies on `workspace/applyEdit` reverse-request `WorkspaceEdit` text instead, exploiting a mutation that injects an in-memory-only call site (`_TEST_CALL = plus(1, 2)`) — its inlined result `_TEST_CALL = 1 + 2` proves in-memory read. | Confirmed via `inspect.getsource(pylsp_rope.plugin.pylsp_code_actions)` + `CommandRefactorExtractMethod.get_code_actions`: titles are hardcoded dictionary keys. | P2 (organize-imports) and any future P-spike that intends to classify pylsp-rope behavior on action title — must use WorkspaceEdit text or executeCommand result inspection instead |
| 2026-04-24 | **pylsp-rope returns command-typed code actions, NOT deferred-resolution**: `refactoring.py:72-81` constructs `CodeAction(title=..., kind=..., command=Command(command="pylsp_rope.refactor.X", arguments=[...]))` directly; the response is fully populated on `textDocument/codeAction`, no `codeAction/resolve` round-trip needed. **Symmetric to S3's rust-analyzer finding** (which is the reverse — rust-analyzer requires resolve). Stage 1A code-action flow MUST detect server style: rust-analyzer needs resolve-then-classify; pylsp-rope works without. | Verified during P1: 20 actions surfaced with `command:` field already populated, `data:` field absent. Inline executed via `workspace/executeCommand` directly. | Stage 1A code-action facade design (must support both styles) + S2/S4/S5/S6 (rust-analyzer side) and P2/P3/P4 (pylsp side) |
| 2026-04-24 | **pylsp-rope edits arrive via `workspace/applyEdit` reverse-request, NOT via executeCommand result**: `pylsp_rope/refactoring.py:CommandRefactorInline.__call__` invokes `workspace.apply_edit(workspace_edit)` (`pylsp/workspace.py`), which sends a `workspace/applyEdit` request from server to client; the executeCommand response is `null`. **Validates S3 outcome B's "minimal `{applied: true, failureReason: null}` stub" decision for the pylsp-rope case** — the stub is mandatory here (without it, rope discards the edit), but the edit is captured client-side. Stage 1A `applyEdit` handler MUST capture the WorkspaceEdit payload (not just acknowledge), since pylsp-rope ships its result there. | Verified during P1: 1 `workspace/applyEdit` reverse-request fired carrying `_TEST_CALL = 1 + 2` newText. | Stage 1A `applyEdit` handler design (S3 minimal-stub decision needs upgrading: stub must intercept + persist the WorkspaceEdit, not just ACK) + Stage 1E pythonStrategy (rope-driven refactors land via this path, not via executeCommand return) |
| 2026-04-24 | **ruff publishes organize-imports under hierarchical sub-kind `source.organizeImports.ruff`, NOT exact `source.organizeImports`**. ruff's `codeActionKinds` capability lists `[quickfix, source.fixAll.ruff, source.organizeImports.ruff, notebook.source.fixAll.ruff, notebook.source.organizeImports.ruff]`. Filtering by client-side `only: [source.organizeImports]` still matches per LSP §3.18.1 prefix rule. Stage 1D `multi_server.py` action-merger MUST treat `kind == "source.organizeImports"` and `kind.startswith("source.organizeImports.")` as the SAME logical action for priority-table purposes; otherwise the merge rule will miss ruff's action entirely. | Verified during P2 against ruff 0.14.6 server. | Stage 1D `multi_server.py` priority-table key normalization (LSP code-action kinds are dot-hierarchical) |
| 2026-04-24 | **`source.organizeImports` semantics differ between pylsp-rope and ruff: pylsp-rope removes unused imports, ruff only sorts/groups them.** On the same polluted seed (`os` + `sys` + `List` prepended), pylsp-rope's organize-imports output is 296 bytes (all 3 unused imports gone); ruff's is 340 bytes (all 3 retained, just a blank line inserted). Ruff's unused-import removal lives under `source.fixAll.ruff`, not `source.organizeImports.ruff`. **Implication for Stage 1D merge:** even though ruff wins per §11.1 priority table, users on pure-ruff get *strictly weaker* behavior than pure-pylsp-rope under `source.organizeImports`. Two MVP options: (a) bind `source.organizeImports` → ruff's `source.organizeImports.ruff` + `source.fixAll.ruff` chained (matches pylsp-rope behavior; +1 round-trip); (b) document the gap and let users invoke `source.fixAll` separately. v1.1 `engine: {ruff, rope}` knob lets users opt back into pylsp-rope's stronger semantics. | Verified during P2: P2.md captures full byte-level diff between the two outputs. | Stage 1D `multi_server.py` design (chain-vs-document decision for organize-imports) + v1.1 strategy config knob design |
| 2026-04-24 | **Wrapper gap (mirrors P1 finding for ruff):** `SolidLanguageServer` has no Ruff adapter in `src/solidlsp/language_servers/`. Stage 1E `PythonStrategy` must add a `RuffServer(SolidLanguageServer)` adapter (template: `jedi_server.py`, ~50 LoC) alongside the `PylspServer` adapter. Until then, all P-spikes that hit ruff drop to the new `_ruff_client.py` raw-stdio helper (105 LoC, parallel to `_pylsp_client.py`). | Confirmed during P2: extracted parallel raw-stdio client to `vendor/serena/test/spikes/_ruff_client.py` for re-use by P5a / P-WB. | Stage 1E scope (add `RuffServer` adapter; ~50 LoC) + P5a / P-WB spike implementers |
| 2026-04-24 | **pylsp-mypy is dropped from the MVP active Python LSP set.** Q1 had pre-decided the canonical configuration (`live_mode: false` + `dmypy: true` + scalpel-injected per-step `didSave`) and named P5a as the falsifier; P5a measurements failed the falsifier on both axes (stale_rate 8.33% ≥ 5%; p95 latency 8.031s ≥ 3s). Root cause: pylsp-mypy's first `didSave` after `didOpen` does not publish `mypy`-sourced `publishDiagnostics` within 8s — dmypy daemon cold-start cost is paid synchronously inside the lint hook, but the diagnostic delivery path (pylsp 500ms debounce + dmypy spin-up + first incremental check) blows the latency budget. Steps 2-12 are flawless (oracle == pylsp-mypy on every comparison once warm), so the failure mode is purely cold-start. **Decision:** scrap the +135 LoC `python_strategy.py`/`multi_server.py` synthetic-didSave plumbing budgeted by Q1 §7; basedpyright becomes the sole type-error source per MVP §11.1; specialist-python.md §3.5 must update the pylsp config block to `"pylsp_mypy": {"enabled": False}`. | P5a evidence: first didSave latency 8.031s vs. p95 ceiling 1s (A) / 3s (B); subsequent 11 latencies all <3.1s with one outlier at 3.04s on step 11 (not investigated; mid-run dmypy invalidation suspected). 11/12 steps converge with oracle=pylsp on error counts. | MVP scope (drop pylsp-mypy entirely); specialist-python.md §3.5 (config block update); MVP report §13 (P5a row → C); CHANGELOG (note basedpyright is sole type-error source); Stage 1E `PythonStrategy` (no synthetic-didSave plumbing needed). |
| 2026-04-24 | **Wrapper gap (P5a):** `SolidLanguageServer` exposes no `notify_did_change_configuration` facade. P5a sends `workspace/didChangeConfiguration` via raw `client.notify(...)` on `PylspClient`. Even if pylsp-mypy were retained at MVP, Stage 1E `PylspServer` adapter would need a thin wrapper method. Recorded for completeness even though pylsp-mypy is being dropped — pylsp-rope/pylsp-ruff configuration may still need this in v1.1+. | Verified by grep over `src/solidlsp/ls.py` — `notify_did_change_*` facades cover open/change/save/close but not `didChangeConfiguration`. | Stage 1E `PylspServer` adapter design (add `notify_did_change_configuration` if any pylsp plugin gains config-driven behavior post-MVP) |
| 2026-04-24 | **Submodule pre-commit hook now blocks commits to `main`/`develop`** (added 23:24 PT after P2's last submodule commit). P5a workaround: created `feature/spike-p5a` branch in the submodule, committed there, then `git checkout main && git merge --ff-only feature/spike-p5a` to keep main pointer-compatible with the parent's submodule SHA reference. | Hook installed by `ensure-git-repo-setup` skill or similar; protects against direct main commits per project CLAUDE.md "Use git flow for all git/github actions". | All future submodule-touching tasks must create a feature branch in the submodule, commit, then ff-merge to main before bumping the parent submodule pointer. |
| 2026-04-24 | **rust-analyzer honors `experimental.snippetTextEdit: false` for ALL surfaced assists on the seed crate**: 43 resolved code actions across a 10-range sweep over `lib.rs` (functions, struct, impl, mod, `pub use`, tests), 0 emit `$N` / `${N` snippet markers. Defensive `$N` strip in `WorkspaceEditApplier` (~10 LoC regex) is sufficient at MVP; the budgeted +50 LoC mandatory-strip path is NOT required. | Verified during S2 by monkey-patching `RustAnalyzer._get_initialize_params` on the instance pre-`start_server()` to flip `experimental.snippetTextEdit: True` -> `False`, then issuing `textDocument/codeAction` + `codeAction/resolve` and scanning every `documentChanges[*].edits[*].newText` (and `changes[uri][*].newText`) with regex `r"\$\d+|\$\{\d+"`. | Stage 1A snippet-strip implementation (defensive only; no mandatory strip needed) + S4/S5/S6 spike implementers (no need to re-verify snippet stripping under richer fixtures unless an offender surfaces). |
| 2026-04-24 | **Wrapper gap (S2):** the fork hard-codes `experimental.snippetTextEdit: True` at `vendor/serena/src/solidlsp/language_servers/rust_analyzer.py:458`; there is NO public capability-override hook on `SolidLanguageServer` / `RustAnalyzer`. The S2 spike monkey-patches `_get_initialize_params` on the instance before `start_server()` to flip the flag. Stage 1A must expose a per-language capability-override knob (e.g., `LanguageStrategy.override_initialize_params(params: InitializeParams) -> InitializeParams`) so scalpel advertises `false` cleanly without monkey-patching. | Verified during S2: confirmed no override hook via `grep -rn "snippetTextEdit\|client_capabilities" src/solidlsp/`; only the static method on the rust subclass populates `experimental.snippetTextEdit`. omnisharp also hard-codes `snippetTextEdit: true` (in JSON file, not Python), suggesting the same gap exists for any other LSP that gates on this capability. | Stage 1A scope (add capability-override hook on `SolidLanguageServer` or per-`LanguageStrategy`; ~+15 LoC plumbing) + any future spike that needs to flip a non-default capability. |
| 2026-04-24 | **`experimental/ssr` is NOT enabled in rust-analyzer 1.95.0 stock build** (server returns LSP error `-32601` "unknown request" for `workspace/executeCommand` with `command: "experimental/ssr"`). The decision (`max_edits: int = 500` default on `scalpel_rust_ssr`) was pre-fixed by plan §13 and is unaffected, but the facade MUST runtime-probe the command before exposing it; otherwise users on stock builds get an opaque `-32601`. Mitigation options for v1.1: (a) document a server-side config knob to enable SSR; (b) fall back to a custom AST-rewrite path; (c) require users to run rust-analyzer with `--experimental` flag if such exists. | Verified during S4 against `rust-analyzer 1.95.0 (59807616 2026-04-14)`: stderr emits `unknown request: ... method: "workspace/executeCommand", params: {"command": "experimental/ssr", ...}`. Argument shape was correct per rust-analyzer LSP-extensions doc (`[{query, parseOnly, selections}]`). | Stage 1B `scalpel_rust_ssr` design (add capability probe + graceful error path; ~+30 LoC) + Stage 1A `execute_command` facade (S3 wrapper-gap reconfirmed by S4). |
| 2026-04-24 | **Spike S4 fix loop — added LSP-code classifier and dynamic RA version capture per quality reviewer Important findings.** (1) Replaced `except Exception` with `except SolidLSPException` and added `_classify_failure(exc) -> (failure_class, lsp_code)` that parses JSON-RPC `-326XX` codes from the message string and maps `32601→feature-unavailable`, `32602→test-bug-invalid-params`, `32603→server-crash-internal-error`, with `timeout`/`unknown-failure` fallback. (2) Replaced hard-coded `rust-analyzer 1.95.0 (59807616 2026-04-14)` literal in the outcome-doc f-string with `_capture_rust_analyzer_version()` shelling out to `rust-analyzer --version` (5s timeout, returns `"unknown"` on `FileNotFoundError`/`TimeoutExpired`/`OSError`). Re-run on the same host preserves outcome (`feature-unavailable (LSP -32601)`, ra_version captured as `rust-analyzer 1.95.0 (59807616 2026-04-14)`); S1+S2 regression checks pass. | Quality reviewer flagged (a) bare `except Exception` lumps LSP method-not-found, timeout, process crash, and test bugs into one bucket making future regressions undiagnosable; (b) hard-coded version string makes the outcome doc lie when run on a different host. Both are now structured fields (`**Failure classification:** failure_class=…, lsp_code=…`) suitable for grep + downstream tooling. | All future spikes that wrap LSP requests in try/except — adopt the `_classify_failure` pattern. Stage 1B `scalpel_rust_ssr` capability probe should reuse the same 32601 detection. Outcome-doc convention: never hard-code tool versions; capture at runtime so cross-host re-runs stay honest. |
| 2026-04-24 | **`rust-analyzer/expandMacro` is NOT registered in rust-analyzer 1.95.0 stock build either** (mirrors S4's `experimental/ssr` finding). Both `decl_macro!(zero)` (declarative `macro_rules!`) and `#[derive(Debug, Clone)]` (proc-macro) positions return LSP `-32601` "unknown request". The declarative-only-vs-both question (S5's stated outcome split A vs B) is **unanswerable on this build**; classifier returns `feature-unavailable` for both positions. Same mitigation surface as S4: gate `scalpel_rust_expand_macro` behind a runtime capability probe. The 32601 detection logic is now reused across S4 + S5 — Stage 1A's `execute_command` facade should ship a single `_method_supported(command: str) -> bool` helper that probes once at session start and caches. | Verified during S5 against `rust-analyzer 1.95.0 (59807616 2026-04-14)`: stderr emits two `unknown request: ... method: "workspace/executeCommand", params: {"command": "rust-analyzer/expandMacro", ...}` lines (one per probed position). Argument shape was correct per rust-analyzer LSP-extensions doc (`[{textDocument, position}]`). | Stage 1B `scalpel_rust_expand_macro` design (capability-probe gate; same pattern as `scalpel_rust_ssr`) + Stage 1A `execute_command` facade (consolidate probe-and-cache). |

## Problems / blockers log

(none yet)

## Spike outcome quick-reference

| Spike | Outcome | LoC delta vs. optimistic |
|---|---|---|
| S1 | A (with shim caveat) | +30 LoC (notification-tap shim per plan §13 fallback) |
| S2 | A | 0 LoC for snippet-strip path (defensive `$N` regex in applier ~10 LoC suffices). +X LoC for Stage 1A capability-override hook (per-language `initialize_params` knob — wrapper-gap finding; rust-analyzer subclass hard-codes `experimental.snippetTextEdit: True`). |
| S3 | B (with re-verify caveat) | -50 LoC vs. optimistic A (~+20 LoC stub instead of ~+80 LoC handler); pending S4/S5/S6 confirmation |
| S4 | feature-unavailable (rust-analyzer 1.95.0 stock) | 0 LoC for `max_edits` (decision pre-fixed: `int = 500` default); +X LoC for Stage 1A `execute_command` facade (S3 wrapper-gap reconfirmed); +Y LoC for Stage 1B `scalpel_rust_ssr` runtime capability probe (returns graceful error on builds without `experimental/ssr` enabled). edit_count=0, rss_delta=80kB on seed (LOWER BOUND). |
| S5 | feature-unavailable (rust-analyzer 1.95.0 stock) | 0 LoC for proc-macro expansion path (decision: facade unavailable on this build); +X LoC for Stage 1A `execute_command` facade (S3/S4 wrapper-gap reconfirmed); +Y LoC for Stage 1B `scalpel_rust_expand_macro` runtime capability probe — gate behind same probe used for `scalpel_rust_ssr`. Both proc-macro and `macro_rules!` positions return `-32601`; declarative-vs-proc-macro question is unanswerable on stock rust-analyzer. |
| S6 | — | — |
| P1 | A | 0 LoC (no extra didSave required); +227 LoC test bypasses wrapper via raw stdio JSON-RPC (pylsp adapter missing — see decisions log) |
| P2 | DIVERGENT (ruff wins per §11.1) | 0 LoC for pylsp-rope organize-imports (dropped at merge); +20-50 LoC for `multi_server.py` disambiguation rule (Stage 1D scope); +349 LoC test/client bypasses wrapper via raw stdio JSON-RPC (pylsp + ruff adapters missing) |
| P3 | — | — |
| P3a | — | — |
| P4 | — | — |
| P5a | C (drop pylsp-mypy) | -135 LoC vs. budgeted (Q1 §7 budgeted ~135 LoC across `python_strategy.py`/`multi_server.py`/tests for the synthetic-didSave path; outcome C means none of that lands at MVP). +11 LoC `_pylsp_client.py` `diagnostics_by_uri` capture (re-usable for P3/P4/P6); +184 LoC P5a test bypasses wrapper via raw stdio JSON-RPC (no `notify_did_change_configuration` facade in SolidLanguageServer). Stale_rate **8.33%** (1/12, step-1 cold-daemon stale); **p95 = 8.031s** (cold-daemon timeout dominates). |
| P6 | — | — |
| P-WB | — | — |
| **Total** | **TBD** | **TBD vs. budgeted +250 LoC** |

## Stage 1 entry verdict

⏳ pending — set after Task 16 (SUMMARY.md) completes.

---

## How to update this file

After every task transition (start → end, status change, commit, decision):

1. Update the **Per-task ledger** row (status emoji + commit hash + one-line outcome).
2. If a decision was made, append to **Decisions log**.
3. If a problem hit, append to **Problems / blockers log**.
4. If a spike completed, fill the **Spike outcome quick-reference** row.
5. Commit this file alongside the work.

Subagents must read this file first when (re-)dispatched and must update it before reporting DONE.
