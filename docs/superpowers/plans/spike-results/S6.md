# S6 - auto_import resolve shape (edit: vs command:)

**Outcome:** A - all 1 auto_import actions carry `edit:` after resolve (edit_only=1, both=0); applier branches on edit only

**Evidence:**

- Code actions surfaced (HashMap range on polluted didChange buffer): 3
- Resolved actions (after `codeAction/resolve`): 3
- auto_import-filtered actions (title 'import' or kind startswith `quickfix.import`): 1
- Shape tally: edit_only=1, command_only=0, both=0, neither=0
- HashMap token range: line=61, char=23..30

**Auto_import actions (up to 10):**

- `Import `std::collections::HashMap`` (kind='quickfix', shape=edit_only)

**API audit (2026-04-24):**

- Polluted buffer delivered via `textDocument/didChange` only (full-doc content change); no disk write, no didSave. fb.version bumped + fb.contents synced for client-side parity.
- S3 finding applied: every action goes through `codeAction/resolve` BEFORE classification.
- Wrapper-gap (S3/S4/S5 confirmed): no `request_code_actions` / `resolve_code_action` facade on `SolidLanguageServer`; test uses `srv.server.send_request(...)` directly.

**Decision:**

- A -> applier branches on `edit:` only (+0 LoC vs. optimistic).
- B -> applier handles both shapes (+40 LoC: edit -> WorkspaceEditApplier; command -> executeCommand + applyEdit-stub WorkspaceEdit capture).
- INDETERMINATE -> re-run with longer indexing budget or richer pollutant; assume B for safety.
