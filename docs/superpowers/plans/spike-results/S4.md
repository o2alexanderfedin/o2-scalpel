# S4 - experimental/ssr WorkspaceEdit upper bound (NON-BLOCKING)

**Outcome:** feature-unavailable (LSP -32601) - SolidLSPException: Error processing request workspace/executeCommand with params:
{'command': 'experimental/ssr', 'arguments': [{'query': '$a + $b ==>> $a.checked_add($b)', 'parseOnly': False, 'selections': []}]}
(caused by unknown request (-32601))

**Failure classification:** failure_class=`feature-unavailable`, lsp_code=`32601`

**Pattern:** `$a + $b ==>> $a.checked_add($b)`

**Evidence:**

- rust-analyzer version: rust-analyzer 1.95.0 (59807616 2026-04-14)
- Edit count (sum of `documentChanges[*].edits[*]` + `changes[uri][*]`): 0
- RSS delta (process, kB): 96
- Raw response JSON bytes: 0
- Error (if any): SolidLSPException: Error processing request workspace/executeCommand with params:
{'command': 'experimental/ssr', 'arguments': [{'query': '$a + $b ==>> $a.checked_add($b)', 'parseOnly': False, 'selections': []}]}
(caused by unknown request (-32601))

**Raw response (truncated to 1500 bytes):**

```json
(empty / null)
```

**API audit (2026-04-24):**

- `experimental/ssr` invoked via `workspace/executeCommand`; arguments shape per rust-analyzer LSP-extensions doc: `[{query, parseOnly, selections}]`.
- Wrapper-gap (S3 confirmed): `SolidLanguageServer` has no `execute_command` facade. Stage 1A must add a thin wrapper method.
- rust-analyzer stock build returns LSP error `-32601` ("unknown request") for `experimental/ssr` -> the assist is gated behind a non-default config or build flag in this release. Stage 1B `scalpel_rust_ssr` MUST runtime-probe the command before exposing it.
- Seed crate has only 2 `+` sites (`add` body + `add_works` test), so any successful edit count would be a LOWER BOUND. Production workspaces (10K-100K LoC) much larger.

**Decision (fixed per plan §13):**

- Ship `max_edits: int = 500` parameter on `scalpel_rust_ssr` facade. Default = `max(500, observed_count * 4)` = `max(500, 0 * 4)` = `500`.
- Seed-crate measurement is a LOWER BOUND; production workspaces require per-call override.
- Feature-unavailable on rust-analyzer stock build: gate `scalpel_rust_ssr` behind a runtime capability probe; return a graceful error on builds without SSR enabled.
