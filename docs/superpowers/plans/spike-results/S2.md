# S2 - snippetTextEdit:false honored by rust-analyzer assists

**Outcome:** A - 0 of 43 resolved actions emit snippet markers; defensive strip is sufficient

**Evidence:**

- Code actions surfaced (10-range sweep over lib.rs): 43
- Resolved actions (after `codeAction/resolve`): 43
- Snippet-marker offenders (`$N` / `${N`): 0
- Capability advertised: `experimental.snippetTextEdit = false` (instance monkey-patch of `RustAnalyzer._get_initialize_params`)

**Offenders:**

_(none)_

**API audit (2026-04-24):**

- Fork hard-codes `experimental.snippetTextEdit: True` at `rust_analyzer.py:458`. NO public capability-override hook on `SolidLanguageServer`/`RustAnalyzer`; spike monkey-patches `_get_initialize_params` pre-`start_server()`. Stage 1A must expose a per-language capability override so scalpel advertises `false`.
- Deferred-resolution code actions (S3): every action MUST go through `codeAction/resolve` before `edit:` is materialized.
- Snippet regex `r"\$\d+|\$\{\d+"` covers `$0` placeholders + `${0:label}` / `${1|a,b|}` choices.

**Decision:**

- A -> defensive `$N` strip in applier (~10 LoC) suffices; no extra work.
- B -> mandatory regex strip path with placeholder/choice/escape edge-cases (+50 LoC).
