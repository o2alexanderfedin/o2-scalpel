# S1 - multilspy `$/progress` forwarding

**Outcome:** A - $/progress reaches dispatcher with rich indexing-class tokens

**Evidence:**

- Dispatcher-probe events: 170
- Distinct tokens: ['rust-analyzer/flycheck/0', 'rustAnalyzer/Building CrateGraph', 'rustAnalyzer/Building compile-time-deps', 'rustAnalyzer/Fetching', 'rustAnalyzer/Loading proc-macros', 'rustAnalyzer/Roots Scanned', 'rustAnalyzer/cachePriming']
- Indexing-class token observed: True
- Public-API probe events (post-yield): 0
- documentSymbol response: DocumentSymbols (truthy=True)

**API audit (verified 2026-04-24):**

- `LanguageServerProcess.on_notification` (`src/solidlsp/ls_process.py:507`) is single-callback-per-method; callback receives `params` only.
- `rust_analyzer.py:720` pre-registers `do_nothing` for `$/progress`; post-yield client taps cannot recover events emitted during init.
- Wrapping `server._notification_handler` BEFORE `start()` confirms the JSON-RPC layer DOES forward every `$/progress` packet to the wrapper.

**Decision:**

- A -> Stage 1A adds +30 LoC notification-tap shim per plan section 13 fallback (additive subscriptions, no clobbering); wait_for_indexing() watches rustAnalyzer/{Fetching,Building,...} and rust-analyzer/flycheck/N.
- B -> ship begin/end coverage; defer fine-grained to v0.2.0.
- C -> shim must tee in the JSON-RPC reader, not at dispatch.
