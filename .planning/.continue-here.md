---
context: phase
phase: v1.6+v1.7-stub-facade-fix
task: 7
total_tasks: 7
status: milestone_complete
last_updated: 2026-04-29T17:07:42-0700
---

# v1.6 + v1.7 — Stub Facade Fix Milestone — COMPLETE

> **Status**: All 7 PRs shipped via adversarial 3-stage subagent pipeline. No in-flight work. Awaiting user direction for next milestone.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Lying-to-LLM `applied=True` while disk untouched | `_dispatch_via_coordinator` recorded `applied={"changes": {}}` and returned `applied=True`; same pattern surfaced in `_dry_run_one_step` returning hardcoded empty `StepPreview` | blocking | Drift-CI gate at `vendor/serena/test/integration/test_facade_docstring_invariants.py` AST-scans `Scalpel*Tool` classes for `del <param>` lines and asserts `informational` opener-tag in docstring. Run `uv run pytest test/integration/test_facade_docstring_invariants.py` before any facade refactor |
| Empty `snapshot={}` in `record_checkpoint_for_workspace_edit` | Every applier was passing empty dict → `inverse_workspace_edit` would write empty content back on rollback | blocking | `apply_action_and_checkpoint` and `apply_workspace_edit_and_checkpoint` helpers in `serena.tools.facade_support` always populate real snapshot via `capture_pre_edit_snapshot` first |
| Stale hardcoded test counts post-Stream6 | `test_installer_registry_has_seven_entries` and similar broke as registry grew | advisory | Generalize set-comparison tests to lower-bound + core-set subset rather than equality |
| Golden tree drift on every commit | Templates embed engine SHA via `git rev-parse HEAD` — every submodule commit drifts the snapshot | advisory | `test_stage_1j_t10_golden.py` uses `_SHA_PATTERN` masking before byte comparison |
| Pre-edit `_inverse_applier_to_disk` partial state | A prior session left a partial inverse-applier impl on disk; PR 7 agent verified it matched spec before adding wiring | advisory | When resuming a feature branch, always verify ALL existing modifications match the active plan before extending |

<current_state>
v1.6 + v1.7 milestone complete. 7 PRs shipped via adversarial 3-stage subagent pipeline.

- Submodule main: `b7abab7b` (tag `v1.7-p7-rollback-inverse-applier`)
- Parent main: `d0a7a75d` (tag `v1.7-p7-rollback-inverse-applier`)
- v1.6 milestone tag on both: `v1.6-stub-facade-fix-complete`
- 401/401 targeted v1.6+v1.7 regression tests green
- pyright 0/0/0 on all touched modules
- Working tree clean on both repos

The 4-STUB / 18-HYBRID / 22-INTENTIONAL audit verdict has been fully addressed:
- 4 STUBs: all closed (apply_capability, split_python, dry_run_one_step, rollback)
- 18 HYBRIDs: addressed via PR 6 doc batch (22 informational-param tags + 2 rollback success-contract docstrings)
- Drift-CI gate added (`test/integration/test_facade_docstring_invariants.py`) to prevent regression
</current_state>

<completed_work>
## v1.6 (6 PRs)
- PR 1 (Plan 0): R2 import-cycle break — `facade_support.py` lifted; lazy-imports gone — tag `v1.6-p0-refactor-cycle`
- PR 2 (Plan 1): Snapshot capture + apply_action_and_checkpoint helper — 9 sites consolidated — tag `v1.6-p1-snapshot-capture`
- PR 3 (Plan 2): Fix scalpel_apply_capability false success — CAPABILITY_NOT_AVAILABLE gate added — tag `v1.6-p2-apply-capability`
- PR 4 (Plan 3): Fix split_python apply to disk + groups[*] warnings — tag `v1.6-p3-split-python`
- PR 5 (Plan 4): Fix dry_run_one_step real preview — 3 projection helpers — tag `v1.6-p4-dry-run-real`
- PR 6 (Plan 5): Doc batch (22 informational tags) + 4 dry_run honoring + 3 threadings + drift-CI gate — tag `v1.6-p5-doc-batch`
- Milestone tag on both repos: `v1.6-stub-facade-fix-complete`

## v1.7 (1 PR)
- PR 7 (Plan 3-A previously deferred): Real rollback inverse-applier — `_inverse_applier_to_disk` + `inverse_apply_checkpoint` + Checkpoint.reverted idempotency flag — tag `v1.7-p7-rollback-inverse-applier`

## Pre-existing fixes (alongside v1.5/v1.6 regression sweeps)
- 5 stale Stage-1J / install_lsp tests fixed (post-v1.4 polyglot + post-v1.2.2 marketplace relocate)
- 3 deeper stale tests (tool param types anyOf, t10 golden SHA mask, calcpy_circular fixture creation)
- pylsp -32601 codeAction/resolve handled gracefully in `solidlsp.ls.SolidLanguageServer.resolve_code_action`
</completed_work>

<remaining_work>
**No in-flight work.** All Stage 3 plans from `IMPLEMENTATION-PLANS.md` shipped, plus the v1.7 deferred Path A.

## Gated next milestones (NOT to be picked up without trigger)
- **Rope-bridge symbol-list semantics** for `_split_python` — gate: concrete fixture demand for symbol-grouped split
- **Phase 4 horizontal facade expansion** to Go/TS/C++/C#/Lean/SMT2/Prolog/ProbLog — gate: +10pp uplift over 53.3% v1.5 routing baseline AND 3 independent user requests per facade
- **Shadow-workspace simulation** in `_dry_run_one_step` — gate: explicitly deferred per SHIP-B; un-defer if real preview misalignment surfaces
- **v2.0 facade ceiling at 36/40** — gate: 3 user requests per new facade

## User-side blockers (orthogonal to engineering)
- PyPI trusted-publisher config — GH settings → Secrets/Variables → Actions
- GH Actions billing top-up — blocks rust-plugin-e2e.yml CI workflow runs
</remaining_work>

<decisions_made>
- Used adversarial 3-stage subagent pipeline (skeptic+defender+synthesizer; over-planner+minimalist+synthesizer; per-PR TDD subagent with self-review) per user directive — single source of truth: `docs/superpowers/plans/2026-04-29-stub-facade-fix/IMPLEMENTATION-PLANS.md`
- Promoted PR 5 (dry_run_one_step) from DEFER to P1 — same lying-to-LLM pattern as apply_capability; SHIP-B deferred shadow-workspace, NOT hardcoded empty StepPreview
- Shipped PR 7 (rollback Path A) despite v1.7 deferral — user explicit "resume Stage 3" directive; PR 2 snapshot helper had unblocked it
- Deferred Examples blocks per minimalist — spec § 5.2.1 routes on PREFERRED:/FALLBACK: not Examples; ~700 LoC zero benchmark signal
- Deferred rope-bridge symbol-list semantics — `groups[*]` is informational in v1.6 with explicit warning emit
</decisions_made>

<blockers>
None. All work checkpointed and pushed to both repos.
</blockers>

## Required Reading (in order, for the resuming agent)
1. `docs/superpowers/research/2026-04-29-stub-facade-audit/STUB-FACADE-AUDIT.md` — the 4 STUB / 18 HYBRID / 22 INTENTIONAL verdict
2. `docs/superpowers/plans/2026-04-29-stub-facade-fix/IMPLEMENTATION-PLANS.md` — the 6-PR roadmap (now all shipped) + § Deferred items with un-defer triggers
3. `~/.claude/projects/-Volumes-Unitek-B-Projects-o2-scalpel/memory/project_v1_6_v1_7_stub_facade_fix.md` — milestone summary
4. `vendor/serena/test/integration/test_facade_docstring_invariants.py` — the drift-CI gate
5. `CLAUDE.md` — project mandates (TDD, gitflow with `--no-ff`, author=`AI Hive(R)`, YAGNI/KISS/DRY)

## Infrastructure State
- Submodule `vendor/serena` uses `feature/* → main` directly (no develop branch)
- Parent `o2-scalpel` uses `feature/* → develop → main` with `--no-ff` merges
- Tags duplicated on both repos for milestone discoverability
- Pre-existing host-binary LSP gaps (haxe/kotlin/lean4/powershell/fsharp/zig/systemverilog) — not v1.6/v1.7 related, operational `-m` filter recommended
- pylsp v1.14.0 lacks `codeAction/resolve` advertisement — `solidlsp.ls.SolidLanguageServer.resolve_code_action` catches `-32601` gracefully

<context>
This pipeline followed the user's explicit directive: "Use a few specialized adversarial agents with superpowers skills to review the code and find out facades with stubs, and synthesize report doc. Then plan to fully implement and test those facades. Then execute the implementation plans. Work in adversarial manner following TDD-like and pair-programming-like way at each and every stage."

The 3-stage pattern — adversarial review (skeptic+defender), adversarial planning (over-planner+minimalist), TDD execution (per-PR subagent with self-review) — proved high-leverage. The synthesizer step at each stage was load-bearing: it forced decisive verdicts on contested calls (e.g., `_dry_run_one_step` deferral status, snapshot capture vs full Path A rollback, Examples-block coverage) rather than letting both views ship simultaneously.

The drift-CI gate at `test/integration/test_facade_docstring_invariants.py` is the durable mechanism that prevents future regressions of the lying-to-LLM pattern. Future facade additions or refactors will be blocked at CI if `del <param>` appears without a corresponding `informational`/`WARNING:` opener-tag.

Future work should follow the same adversarial pattern. Start with a fresh audit (`STUB-FACADE-AUDIT.md` style) on a different surface (e.g., `solidlsp` adapters, marketplace primitives, or the Stage 1H/1J test infrastructure).
</context>

<next_action>
Await user direction for next milestone. Resumption candidates by priority:

1. **If user requests rope-bridge expansion**: Open `docs/superpowers/plans/2026-04-29-stub-facade-fix/IMPLEMENTATION-PLANS.md` § Deferred → "rope-bridge symbol-list semantics for split_python" → request a concrete fixture demand → run audit → plan → execute via 3-stage adversarial pipeline.

2. **If user requests Phase 4 horizontal facade expansion**: Re-run routing benchmark (`uv run pytest test/spikes/test_routing_benchmark.py::test_routing_accuracy_meets_floor -v -s`) and verify `routing_accuracy >= 0.633` (53.3% + 10pp). If gate met, propose specific languages with 3 user-request evidence trail.

3. **If user requests new audit on different surface**: Dispatch parallel adversarial review agents on chosen surface (e.g., `solidlsp` adapters, MCP primitives, test infra) — write reports to `docs/superpowers/research/<date>-<topic>/`.

4. **If user requests final regression sweep on whole submodule**: Run `uv run pytest test/spikes/ test/serena/ test/e2e/ -q --timeout=60 --ignore=test/integration` and triage any new gaps.

Do NOT autonomously start any of the above without an explicit user trigger — the gating discipline is the project's YAGNI mandate.
</next_action>
